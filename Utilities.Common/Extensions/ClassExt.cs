using System;

namespace Utilities.Common.Extensions
{
    public static class ClassExt
    {
        //public static T LazyInitialize<T>(this T FieldVariable) where T : class, new()
        //{
        //    if (FieldVariable == null) FieldVariable = new T();
        //    return FieldVariable;
        //}

        //public static T LazyInitialize<T>(this T FieldVariable, T Result) where T : class
        //{
        //    if (Result == null) throw new ArgumentNullException(nameof(Result));
        //    if (FieldVariable == null) FieldVariable = Result;
        //    return FieldVariable;
        //}
    }
}
